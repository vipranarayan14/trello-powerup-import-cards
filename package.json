{
  "name": "trello-powerup-import-cards",
  "version": "0.1.0",
  "description": "Trello Power-Up for importing cards",
  "source": "src/index.html",
  "scripts": {
    "build": "rm -rf dist && parcel build",
    "netlify": "netlify",
    "direct-deploy": "npm run build && netlify deploy --prod"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "netlify-cli": "^8.9.6",
    "parcel": "^2.2.0"
  }
}
